---
phase: 02-rag-webllm-integration
plan: 01
type: execute
depends_on: []
files_modified: []
---

<objective>
Enable RAG-augmented responses for local WebLLM models by integrating backend hybrid search with frontend in-browser inference

Purpose: Allow users to leverage their document knowledge base with privacy-preserving local models
Output: RAG-enabled local model chat with citations and context display
</objective>

<execution_context>
@.goodflows/workflows/execute-plan.md
@.goodflows/templates/summary.md
</execution_context>

<context>
@.goodflows/PROJECT.md
@.goodflows/ROADMAP.md
@.goodflows/STATE.md
</context>

<tasks>

<task type="auto" id="task-1">
  <name>Create RAG context retrieval service function</name>
  <files>frontend/app/chat/page.tsx, frontend/src/services/ragService.ts</files>
  <action>
    1. Create retrieveRAGContext() function in chat page or as separate service
2. Call backend /api/v1/search/hybrid endpoint with user query
3. Transform search results into structured context format
4. Handle errors gracefully with fallback to no-context mode
  </action>
  <verify>npm run type-check</verify>
  <done>Function successfully retrieves and formats RAG context from backend</done>
</task>

<task type="auto" id="task-2">
  <name>Add RAG toggle UI component and state</name>
  <files>frontend/app/chat/page.tsx, frontend/src/components/chat/RAGToggle.tsx</files>
  <action>
    1. Add enableRAG state to chat page (default: true)
2. Create RAG toggle switch in chat header area near model selector
3. Style toggle to match Terminal Observatory theme (phosphor green)
4. Persist preference to localStorage
  </action>
  <verify>npm run type-check &amp;&amp; npm run lint</verify>
  <done>Toggle renders correctly and persists state</done>
</task>

<task type="auto" id="task-3">
  <name>Integrate RAG context into local WebLLM flow</name>
  <files>frontend/app/chat/page.tsx</files>
  <action>
    1. Modify handleSubmit() to check enableRAG &amp;&amp; !isCloudModel
2. If RAG enabled, call retrieveRAGContext() before WebLLM inference
3. Construct system prompt with retrieved context documents
4. Include context in messages sent to local engine
5. Add loading state for RAG retrieval phase
  </action>
  <verify>npm run type-check</verify>
  <done>Local models receive RAG context and generate contextual responses</done>
</task>

<task type="auto" id="task-4">
  <name>Map RAG results to citations for local models</name>
  <files>frontend/app/chat/page.tsx, frontend/src/types/chat.ts</files>
  <action>
    1. Transform RAG search results into Citation[] format
2. Store citations with assistant messages for local model responses
3. Enable citation preview/display for local model messages
4. Ensure citation links work (document viewer integration)
  </action>
  <verify>npm run type-check</verify>
  <done>Citations display correctly for local model responses</done>
</task>

<task type="checkpoint:human-verify" id="task-5" gate="blocking">
  <what-built>Complete RAG + WebLLM integration</what-built>
  <how-to-verify>
    1. Select a local model (Phi-3.5 or Llama)
2. Enable RAG toggle
3. Ask a question about uploaded documents
4. Verify response includes relevant context
5. Check citations appear and link to source documents
  </how-to-verify>
  <resume-signal>Type &quot;approved&quot; to continue</resume-signal>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] npm run type-check passes
- [ ] npm run lint passes
- [ ] Manual testing with local model + RAG enabled
- [ ] Performance: RAG retrieval < 2s latency
</verification>

<success_criteria>
- Local WebLLM models can access backend document knowledge
- RAG toggle allows users to enable/disable context retrieval
- Citations display for local model responses
- No regression in cloud model RAG functionality
- Graceful fallback when RAG retrieval fails
</success_criteria>