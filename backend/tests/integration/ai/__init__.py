"""
AI Integration Tests Package.

These tests can optionally call real AI providers when configured.
By default, they are skipped unless AI_INTEGRATION_TESTS=true is set.

Run with:
    AI_INTEGRATION_TESTS=true pytest tests/integration/ai/ -v
"""
