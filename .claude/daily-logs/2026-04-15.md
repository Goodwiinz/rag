# Daily Log — 2026-04-15

## Shipped

### 1. Quality metrics trend analysis (TODO → implemented)

- **File**: `backend/src/services/quality/quality_recommendations_service.py`
- `_analyze_relevance_insights()` and `_analyze_response_time_insights()` now compare current period vs previous period to emit `"improving"` / `"declining"` / `"stable"` trends (±5% threshold)
- Period length derived from `cutoff_date` so no new param needed

### 2. Kubernetes hardening (audit + fixes)

- **`infrastructure/kubernetes/manifests/api-server-deployment.yaml`**: removed `http://localhost:3000` from CORS, fixed readiness probe `/docs` → `/health`
- **`infrastructure/helm/.../templates/neo4j-secret.yaml`** (new): stores `NEO4J_AUTH` as K8s Secret instead of plaintext in values
- **`infrastructure/helm/.../values-production.yaml`**:
  - Cleared plaintext neo4j password
  - Fixed `QDRANT_URL` localhost → cluster DNS
  - Added missing `redis-credentials` to `envFrom` (critical — was silencing all 7 caches)
  - Added explicit LLM cache env vars
  - Enabled HPA (backend 2–6, frontend 2–4 replicas)
  - `pullPolicy: Always` → `IfNotPresent`
  - Enabled `networkPolicy: true`
- **`templates/frontend-deployment.yaml`**: pod securityContext, affinity, startupProbe
- **`templates/celery-beat-deployment.yaml`**: liveness probe added
- **`templates/hpa.yaml`**: behavior policies (scale-down stabilization), celery-worker HPA added
- **`templates/pdb.yaml`**: frontend + celery-worker PDBs added
- **`templates/backend-deployment.yaml`**: removed unnecessary `Host: localhost` httpHeaders from all probes, added container securityContext
- **`infrastructure/argocd/applications/staging.yaml`**: removed `automated` sync block

### 3. CI/CD — GitHub Actions workflow lint fix

- Fixed non-existent action versions across 4 workflow files (`@v6`→`@v4`, `@v7`→`@v6`, etc.)
- `Validate GitHub Actions Workflows` CI check now passing

### 4. Caching system hardening (5 improvements)

- **Stampede protection**: Redis NX lock in `@cached` decorator (30s TTL, 500ms poll, double-check after lock acquired)
- **TTL jitter ±15%**: `_jittered_ttl()` applied to every `setex()` in all 7 cache backends
- **gzip compression**: payloads >1 KB compressed with `gz:` prefix in `serialize_value` / `deserialize_value`
- **Search key hash**: 16 → 32 hex chars
- **Prometheus metrics**: `cache_hits_total`, `cache_misses_total`, `cache_errors_total`, `cache_hit_rate`
- **Cache warmup**: new `backend/src/core/cache_warmup.py` → `warm_critical_caches()` background task in `main.py` lifespan

### 5. CI/CD — Self-hosted runner + Spaces Docker cache

- `docker-build.yml`: build jobs → `[self-hosted, linux]`; Docker cache → DO Spaces `rag-system-storage` (persistent, no size cap)
- `infrastructure/arc/runner-deployment.yaml` (new): ARC `RunnerDeployment` + `HorizontalRunnerAutoscaler` (1–3 pods, DinD, 4 CPU / 8 GB)

## Pending (user action required)

- Add GitHub secrets: `DO_SPACES_ACCESS_KEY`, `DO_SPACES_SECRET_KEY`
- Install ARC + apply runner manifest (see `infrastructure/arc/runner-deployment.yaml` header comments)

## Branch

`claude/implement-todo-item-fgVkL` — all changes committed and pushed
