# Code owners. Last match wins, so more specific paths must come after the
# default. See https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners

# Default owner for anything not matched below.
*                 @goodwiins

# Area owners (currently the same person; kept separate so a future
# split-by-area doesn't require restructuring this file).
/backend/         @goodwiins
/frontend/        @goodwiins
/.github/         @goodwiins
/docs/            @goodwiins
