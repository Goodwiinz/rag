{{- if and .Values.neo4j.enabled .Values.neo4j.persistence.enabled }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "rag-system.fullname" . }}-neo4j-pvc
  namespace: {{ include "rag-system.namespace" . }}
  labels:
    {{- include "rag-system.labels" . | set "app" "neo4j" | set "version" "v1.0.0" | nindent 4 }}
spec:
  accessModes:
  - {{ .Values.neo4j.persistence.accessMode | default "ReadWriteOnce" }}
  storageClassName: {{ .Values.neo4j.persistence.storageClass }}
  resources:
    requests:
      storage: {{ .Values.neo4j.persistence.size }}
{{- end }}