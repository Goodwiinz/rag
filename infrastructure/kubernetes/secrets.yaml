# =============================================================================
# Secrets management — see Infisical
# =============================================================================
#
# All cluster secrets are now managed by the Infisical operator (cloud).
# See:
#   - docs/infrastructure/infisical-setup.md          — full setup guide
#   - infrastructure/kubernetes/infisical-bootstrap.md — one-time bootstrap
#   - infrastructure/argocd/applications/infisical-operator.yaml — operator install
#   - infrastructure/helm/knowledge-graph-analytics/templates/infisical-secrets.yaml
#       — InfisicalSecret CRDs that materialize K8s Secrets per env
#
# This file is kept as a pointer; no resources are defined here.
# Earlier revisions contained:
#   - Empty placeholder Secrets (now produced by Infisical, with real values)
#   - An AWS Secrets Manager ExternalSecret config that was never wired up
#     (NOUS runs on DigitalOcean, not AWS) — removed.
