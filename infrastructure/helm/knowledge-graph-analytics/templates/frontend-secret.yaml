{{/*
  Frontend secrets placeholder — satisfies checksum annotation
  in frontend-deployment.yaml. No actual secrets managed here.
*/}}
{{- if .Values.frontend.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "knowledge-graph-analytics.fullname" . }}-frontend-placeholder
  labels:
    {{- include "knowledge-graph-analytics.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
type: Opaque
data: {}
{{- end }}
