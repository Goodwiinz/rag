# Next.js resolves modules from the workspace root in this monorepo.
# Hoisted linking avoids duplicate next/react copies in the client bundle.
node-linker=hoisted
public-hoist-pattern[]=*next*
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*
