Run the tenant-isolation probe matrix: seed two disjoint organizations (org A
with one document, one project, one knowledge-graph entity pair, one saved
memory, and one project-skill entry; org B with none of it), then issue each
of the 12 declared read probes as org B's user with the same query
parameters org A's own probe used, and record both sides' results.
