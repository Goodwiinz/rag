Exercise the human-in-the-loop confirmation lifecycle for a destructive agent
action, across four phases: approve a pending action once, reject a pending
action, re-confirm an action whose interrupt is already resolved, and cancel
while an interrupt is parked awaiting confirmation.
