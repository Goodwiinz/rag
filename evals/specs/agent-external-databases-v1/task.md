Status: approved

Capability: discover the production external-database connector registry and search named connectors (PubMed, FRED) through the real connector wire protocol, grounding the final answer in results the double genuinely served rather than a fabricated citation.

Request: `First call list_external_databases to see which external database connectors are available. Then call search_external_database with connector="pubmed" for the topic "telomere shortening senescent cells", and call search_external_database with connector="fred" for the economic series "unemployment rate". Report what you found from each source.`

Initial conditions: the synthetic user owns one workspace; the PubMed E-utilities and FRED wire routes are served by a deterministic protocol double built to the actual connector parsers, each request recorded. The benchmark provisions `FRED_API_KEY` but deliberately leaves `ALPHA_VANTAGE_API_KEY`/`COSMIC_AUTH` unset, so the real 11-connector registry reports exactly the key-gated connectors that should be unavailable. `search_external_database`/`list_external_databases` are bound to the general intent, so the harness sends the turn through live production classification.

Why this requires the capability: success requires the production graph to expose and correctly dispatch two non-destructive but still state-touching tools — one that reports the true registry (including which key-gated connectors are actually available), and one that fans a query out to the named connector's real wire protocol and returns genuine results. A model that reports a plausible-sounding connector list, invents a search result, or lets the domain fan-out substitute for the explicitly requested connector all fail deterministically.

Pass iff: `list_external_databases` returns `total == 11` with `fred.available == true` and `alpha_vantage`/`cosmic` both `available == false, requires_api_key == true`; `search_external_database` is called with `connector="pubmed"` and again with `connector="fred"` (not a domain fan-out), each returning `connectors_searched` equal to exactly that one connector and result rows whose ids are a subset of the fixture corpus actually served by the double; the double's request log independently shows the PubMed esearch/efetch calls and a FRED `series/search` call carrying the benchmark's provisioned API key; and the final assistant message cites at least one title or identifier genuinely present in each double's response, with no pending tool call and a `completed` termination — and no HITL interrupt occurs, since neither tool is destructive.

Verifier: deterministic identity, network-boundary, real-routing, fresh-turn, registry-truth, and mock-event-provenance gates, plus a literal citation-containment check against the doubled fixtures. No LLM judge is needed or configured — semantic scoring is N/A for this task.

Verifier evidence: the observed graph-state routing intent (must be `general`); fresh-turn message identity evidence; the `list_external_databases`/`search_external_database` tool executions and their full results; the mock double's full request-event log (PubMed esearch/efetch parameters, FRED search parameters including `api_key`); the final assistant message; and termination reason.

Accepted alternatives: query wording, result ordering, and final-message phrasing may vary; only the connector targeting (`connector=` explicit, not a domain fan-out), the registry truth values, and citation grounding in genuinely doubled results are material.
