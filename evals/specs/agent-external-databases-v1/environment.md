Status: approved

Dependencies:

- Production dependency image: private DigitalOcean registry tag `3a436b2-r1`, resolved and recorded as `sha256:75b224f86a60c02e9343ca085fb4f195251a2554119b78e0972c3e09966c0e34`; the pinned repository backend source is copied over `/app` at build time, including the full `src/services/connectors/` registry.
- Azure/OpenAI chat deployments: live, model inference only, using the Harness credential names. Effects are bounded API calls and token cost.
- PostgreSQL: simulated isolated service, initialized from the pinned repository's current SQLAlchemy metadata. Only the synthetic tenant triple (organization/user/workspace) is seeded — this task exercises no document or project state.
- Mock services: a deterministic HTTP double implementing the PubMed E-utilities wire contract (`GET /entrez/eutils/esearch.fcgi`, `GET /entrez/eutils/efetch.fcgi`) and the FRED wire contract (`GET /fred/series/search`), built directly from the parsers in `pubmed.py`/`fred.py` rather than from real upstream behavior. Every request is recorded to a `GET /events` log, including the FRED `api_key` query parameter.
- LangGraph checkpointer: the repository's PostgreSQL checkpointer against the same isolated database.
- Redis, LangSmith, DigitalOcean KB, Cohere, and production Supabase: disabled or network-blocked because the task does not exercise them.

Backend contracts: the production `search_external_database`/`list_external_databases` tools receive the fixed synthetic user/thread context and dispatch through the unmodified `connector_registry`/`_tool_search_external_database`/`_tool_list_external_databases` path — the impl `max_results` cap is `min(max_results, 20)` (`tools_impl.py:2997`) regardless of the wrapper's higher clamp, and connector/domain identifiers are sanitized by `_validate_connector_name` (`tools.py:98`) before dispatch. The registry holds exactly 11 built-in connectors (`pubmed`, `uniprot`, `chembl`, `pubchem`, `sec_edgar`, `fred`, `alpha_vantage`, `zinc`, `cosmic`, `clinical_trials`, `bioservices`); only `fred`, `alpha_vantage`, and `cosmic` declare `requires_api_key=True` (`ExternalDBConnector.is_available()` checks the connector's own `api_key_env_var` against the process environment) — the design doc's "9 unavailable" figure does not match the registered connectors and is superseded by this recon.

Data: one fixed synthetic organization, active user, and owned workspace with neutral UUIDs. User email `benchmark-external-db@example.invalid`. Fixture corpora: two PubMed articles (PMIDs `39012345`/`39012346`) about telomere shortening in senescent cells, and two FRED series (`UNRATE`/`UNRATENSA`) describing the U.S. unemployment rate — both deterministic, checked into `environment/mock_services/fixtures.json`.

Storage and reset: the double's request-event log is recreated with a fresh container before every trial; PostgreSQL is recreated from production model metadata before each trial. The double logs the full submitted query terms and the FRED API key per request (needed to prove genuine dispatch and correct credential use) but no other request bodies contain sensitive content.

Isolation: one database and one graph thread per trial; outbound network allowlisted only to the approved model endpoint and the mock double, both on an internal-only network; no production credentials; real monotonic time; deterministic UUID fixtures.

Current routing contract: the adapter sends a real `initial_agent_state` through production classification. `search_external_database` and `list_external_databases` are bound to `AgentIntent.GENERAL`, so the observed checkpoint intent must be `general`; no sentinel value or `aupdate_state` workaround is permitted. The verifier also proves that the current run appended a fresh human message matching the instruction, rather than reusing a persisted checkpoint message. The historical fidelity paragraph below describes the pre-#1378 workaround and is retained only to explain why this task changed.

Historical pre-#1378 routing note and fidelity limits: Before #1378, the two connector tools were unreachable through live classification and the adapter seeded a sentinel checkpoint state; that workaround is retained here only as history. `pubmed.py`'s `_ESEARCH`/`_EFETCH` module constants are f-string-baked from `_BASE_URL` at import time, so the adapter patches those two module attributes directly rather than `_BASE_URL` (patching `_BASE_URL` alone would do nothing, since the already-baked constants never re-read it); `fred.py`'s `_BASE_URL` is read at call time inside `search()`/`fetch_by_id()`, so patching that one constant is sufficient there. Only `pubmed` and `fred` are doubled; the other 9 registered connectors (7 keyless, 2 key-gated: alpha_vantage, cosmic) are deliberately not exercised — the instruction pins `connector=` explicitly on every search to avoid a no-connector fan-out reaching an un-doubled host. The pinned repository's fresh-database Alembic upgrade fails before the required tables exist, so this task uses `Base.metadata.create_all()` and does not establish migration correctness. It measures whether the production graph safely dispatches and reports the external-database capability, not PubMed/FRED upstream API fidelity.
