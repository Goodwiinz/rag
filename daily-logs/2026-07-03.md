# 2026-07-03 — Daily Code Quality Audit

**Scope:** `backend/src/exceptions/` (day 184 % 44 dirs → index 8 → `exceptions`)
Files reviewed: `__init__.py`, `analytics_exceptions.py`, `error_handlers.py`

## Findings (meaningful, verified via grep)

1. **High — Orphaned error-handling module / dead `RAGException` hierarchy.**
   `setup_error_handlers()` (`backend/src/exceptions/error_handlers.py:284`) is never called anywhere in the codebase. `backend/src/main.py` registers its own inline handlers for `RequestValidationError`, `HTTPException`, and generic `Exception`, but never wires up `RAGException`/`AnalyticsException`/`SQLAlchemyError`. The entire `RAGException` hierarchy in `__init__.py` (~35 classes, e.g. `DocumentNotFoundException`, `AuthenticationException`) is never raised anywhere in `backend/src` — confirmed dead code. Any code that raises one of these classes today would get a generic 500 instead of the documented structured response.

2. **Medium — Duplicate `ConfigurationException` class name with incompatible constructors.**
   `src/exceptions/__init__.py:324` (`RAGException` subclass, `__init__(message, details, cause)`) vs. `src/exceptions/analytics_exceptions.py:148` (`AnalyticsException` subclass, `__init__(config_key, config_value, reason, details)`). Both importable from sibling paths in the same package. Existing call sites use the right one, but an IDE auto-import picking the wrong one would cause a runtime `TypeError` in RBAC/tenant error paths.

## Linear filing — FAILED, needs manual follow-up

Attempted to file both findings via the `issue-creator` subagent (team Goodwiinz) twice. **Both attempts returned transcripts showing `linear_create_issue`/`linear_list_issues` calls, but `tool_uses: 0` in the subagent's usage metadata both times** — i.e. the agent narrated tool calls as text without actually executing them. No Linear MCP server appears in this session's connected/connecting server list (only Context7, Slack, Supabase, github, redis, serena), which is the likely root cause — Linear integration may not actually be wired up in this environment despite the `issue-creator` agent definition listing `linear_*` tools.

**Action needed:** verify Linear MCP connectivity for this environment, then manually file (or re-run the audit's filing step once fixed) the two findings above.
